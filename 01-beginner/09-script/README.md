## Q09: シェルスクリプトを作って実行しよう

この問題では、**RUNでシェルスクリプトを作成し、CMDでそのスクリプトを実行**してもらいます。

## 要件
- ベースイメージ：Alpine Linux
- ビルド時に `/hello.sh` を作成する
  中身の例：
  ```sh
  #!/bin/sh
  echo "Hello from script"
- コンテナ起動時に、hello.shを実行させる
- 権限に気をつけましょう！
